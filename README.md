# Shinsekai-Playwright-Browser
Enable agents to operate your browser

Please run the following commands to download the dependencies:
```
pip install playwright
playwright install chromium
```
If you are using the embeded python, please run the commands in the project root:
```
runtime\pyhton.exe -m pip install playwright
runtime\python.exe -m playwright install chromium
```

